// src/types/Bullet.ts
export interface Bullet {
  id?: string;
  content: string;
  competency: string;
  rankCategory?: string;
  rank?: string;
  // Add any other properties your Bullet type needs
}